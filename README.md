# ModC

WIP.

To preview in mkdocs:

1. `python -m venv venv`
2. `source ./venv/bin/activate`
3. `pip install mkdocs mkdocs-material pygments`
4. `pushd pygments-plugin-modc; pip install .; popd`
5. `cd mkdocs && ../venv/bin/mkdocs serve`
